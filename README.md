# WebBase
🚀 Open-source web project base: .NET API + React frontend + Docker Compose. WebBase provides a ready-to-use fullstack structure to kickstart your next web project.


# 🧠 Purpose

WebBase is not a framework, but a base template to:

Manage authenticated and guest users

Provide a consistent technical layout for web applications

Simplify setup and deployment with Docker

# ⚙️ Tech Stack

Backend: .NET 8 (C# REST API)

Frontend: React + Vite

Database: PostgreSQL (configurable)

Containerization: Docker & Docker Compose

# 🚀 Getting Started

Clone the repository and start everything with Docker:

```bash
git clone https://github.com/sonixray/WebBase.git
cd WebBase
docker-compose up
```

Then open http://localhost:3000 🎉

# 🤝 Contributing

Contributions are welcome!
Fork the repository, create a feature branch, and submit a pull request to improve or extend the base.
